# Frontend

`mockup/index.html` is the interactive design mockup used to validate the dashboard's layout and flows (VM list, Docker/systemd tabs, live-log simulation, VM onboarding). Open it directly in a browser — no build step, no backend required, all data is fake.

This becomes the reference for the real frontend (React, calling the API in `../backend`), planned for phase 2.
